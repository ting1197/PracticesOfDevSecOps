apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: ecommerce
  labels:
    app: order-service
    part-of: ecommerce
spec:
  type: ClusterIP
  selector:
    app: order-service
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
      name: http
